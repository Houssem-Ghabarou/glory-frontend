import React from "react";
import { Item } from "@/types/item";

interface CustomCardProps {
  items: Item[];
}

const CustomCard = ({ items }: CustomCardProps) => {
  return <div></div>;
};

export default CustomCard;
